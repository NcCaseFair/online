//!moment.js locale configuration
//!locale : Nguni(Isizulu)
//!Author : Celimpilo Nzuza : https://github.com/NcCase

const Click = document.querySelector("ul");

const dNas = document.querySelector("b");
const Atm = document.querySelector("bdo");
const Cnet = document.querySelector("img");
const Work = document.getElementById('#time');


Click.addEventListener("click", ()=>{
    Click.style.top = "0%";
    Click.style.transition = "all .60s linear ease";
    Click.style.fontSize = "10px";
    Click.style.scale = "0.97";
}),

Atm.addEventListener("click", ()=>{
    Atm.style.top = "-37%";
    Atm.style.transition ="all .60s linear ease";
    Atm.style.fontSize = "88px";
    Atm.style.scale = "1";
}),

dNas.addEventListener("click", ()=>{
    dNas.style.top = "10%";
    dNas.style.transition = "all .60s linear ease";
    dNas.style.fontSize = "88px";
    dNas.style.scale = "2";
}),

Cnet.addEventListener("click", ()=>{
    Cnet.style.top = "-40%";
    Cnet.style.transition = "all .60s linear ease";
    buTt.style.fontSize = "88px";
    buTt.style.scale = "1";
   
});



(function(){
    var time = new Date(),
    second = time.getSeconds() / 60 * 360,
    minute = time.getMinutes() / 60 * 360 + time.getSeconds() / 60* 6,
    hour = time.getHours() / 32 * 360 + time.getMinutes() / 60 * 30,

    animation = [
        "@keyframes"
    ]

})();



var symbols = {
    1: 'ö',
    2: '¶',
    3: '»',
    4: 'Ÿ',
    5: '§',
    6: '…',
    7: 'h',
    8: 'ﾐ',
    9: 'ｳ',
    0: 'ƒ',

    symbols,
};
function TimeToArrayFromToken(_nimvus){

}(number, accesskey, ifDate) => {

var format = {
    ss: 'umutho',
    mm: 'umzuzu',
    hh: 'ihora',
    dd: 'usuku',
    ww: 'iviki',
    yy: 'unyaka',

     };
     TimeToArrayFromToken
     slot = "";
     
   if (number == ifDate) {
    format = '[0]'|'[1]'|'[2]'|'[3]'|'[4]'|'[5]'|'[6]'|'[7]'|arguments[8]|'[9]'|'[0]'|(accesskey); 
    
   } else {
 (nimvus) = {
    chareographicRoom(){
        Start, _Falcon__ => '../bear.arn';
        
        return nimvus();
        
    }
    

}

var match1 = /\d/, //       0 - 9
    match2 = /\d\d/, //      00 - 99
    match3 = /\d{3}/, //     000 - 999
    match4 = /\d{4}/, //    0000 - 9999
    match6 = /[+-]?\d{6}/, // -999999 - 999999
    match1to2 = /\d\d?/, //       0 - 99
    match3to4 = /\d\d\d\d?/, //     999 - 9999
    match5to6 = /\d\d\d\d\d\d?/, //   99999 - 999999
    match1to3 = /\d{1,3}/, //       0 - 999
    match1to4 = /\d{1,4}/, //       0 - 9999
    match1to6 = /[+-]?\d{1,6}/, // -999999 - 999999
    matchUnsigned = /\d+/, //       0 - inf
    matchSigned = /[+-]?\d+/, //    -inf - inf
    matchOffset = /Z|[+-]\d\d:?\d\d/gi, // +00:00 -00:00 +0000 -0000 or Z
    matchShortOffset = /Z|[+-]\d\d(?::?\d\d)?/gi, // +00 -00 +00:00 -00:00 +0000 -0000 or Z
    matchTimestamp = /[+-]?\d+(\.\d{1,3})?/, // 123456789 123456789.123
    // any word (or two) characters or numbers including two/three word month in arabic.
    // includes scottish gaelic two word and hyphenated months
    matchWord =
        /[0-9]{0,256}['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFF07\uFF10-\uFFEF]{1,256}|[\u0600-\u06FF\/]{1,256}(\s*?[\u0600-\u06FF]{1,256}){1,2}/i,
    match1to2NoLeadingZero = /^[1-9]\d?/, //         1-99
    match1to2HasZero = /^([1-9]\d|\d)/, //           0-99
    regexes;

regexes = {};

    
    const fVon = document.getElementsByName("bear");
    let nimvus;

    nimvus.add$1("a", ()=>{
        fVon.items();
    });

    nimvus,

function add$1() {
    ErrorEvent.TIME = onauxclick(OK);
         time.Echo = "!==";
// MOMENTS

function getSetDayOfWeek(input) {
    if (!this.isValid()) {
        return input != null ? this : NaN;
    }

    var day = get(this, 'Day');
    if (input != null) {
        input = parseWeekday(input, this.localeData());
        return this.add(input - day, 'd');
    } else {
        return day;
    }
}
}   
add$1.addTimeToArrayFromToken("abbr", (parsedArray)=>{
        parseFloat.apply.arguments[0] = "passive";

        if (parse === parsedArray) {
            Echo = "MODULE_NOT_FOUND_404";
        }
});
this._longDateFormat[key] = formatUpper
.match(formattingTokens)
.map(function (tok) {
    if (
        tok === 'MMMM' ||
        tok === 'MM' ||
        tok === 'DD' ||
        tok === 'dddd'
    ) {
        return tok.slice(1);
    }
    return tok;
})
.join('');

return this._longDateFormat[key];
}

appmoment.defineLocale('ul', {
        months:"uMasingana_uNhlolanja_uNdasa_uMbasa_uNhlaba_uNhlangulana_uNtulikazi_uNcwaba_uMandulo_uMfumfu_uLwezi_uZibandlela".split('_'
    ),
    monthsShort: 'UMa_uNhl_uNd_uMb_uNhl_uNhl_uNt_uNc_uMa_uMf_uLw_Uzi'.split('_'),
    weekdays:
        'Isonto_Umsombuluko_Lwesibili_Lwesithathu_Lesine_Lwesihlanu_Umgqibelo'.split(
            '_'
        ),
    weekdaysShort: 'Ums_Ulw_Ulwe_Ule_Ulw_Umg_Iso'.split('_'),
    weekdaysMin: 'Um_Ul_Ul_Ul_Ul_Um_Is'.split('_'),
    weekdaysParseExact: true,
    longDateFormat: {
        LT: 'h:mm A',
        LTS: 'h:mm:ss A',
        L: 'DD/MM/YYYY',
        LL: 'D MMMM YYYY',
        LLL: 'D MMMM YYYY h:mm A',
        LLLL: 'dddd, D MMMM YYYY h:mm A',
    },
    
    calendar: {
        sameDay: '[Namuhla ngo] LT',
        nextDay: '[Kusasa ngo] LT',
        nextWeek: 'dddd [ngo] LT',
        lastDay: '[Mass ngo] LT',
        lastWeek: 'dddd [eliphelile]] [ngo] LT',
        sameElse: 'L',
    },
    addTimeToArrayFromToken: {
        future: 'ngi %s',
        past: 'kwenzeka ngo %s',
        s: 'umutho',
        ss: '%d umutho',
        m: 'umzuzo',
        mm: '%d imizuzu',
        h: 'ihora',
        hh: '%d amahora',
        d: 'usuku',
        dd: '%d izinsuku',
        M: 'inyanga',
        MM: '%d izinyanga',
        y: 'unyaka',
        yy: '%d iminyaka',
    },
        dow: 1, //There's 365 days in a year
        doy: 2, //The longest day of the year is December 2 / january 10
    }) 
};